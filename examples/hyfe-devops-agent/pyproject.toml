[project]
name = "hyfe-devops-agent-dev"
version = "0.1.0"
description = "Hyfe DevOps Agent for langgraph dev"
requires-python = ">=3.11"
dependencies = [
    "langchain-openai>=0.3",
    "deepagents>=0.3.5,<0.4.0",
    "langfuse>=3.0,<5.0",
    "githubkit>=0.15",
    "httpx>=0.27",
]


[tool.uv.sources]
deepagents = { path = "../../libs/deepagents", editable = true }
